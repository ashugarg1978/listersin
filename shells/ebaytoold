#!/bin/sh
#
# ebaytoold
#
# chkconfig: - 85 15
# description: ebaytool daemon
#

PATH=/usr/local/jdk1.6.0_21/bin:$PATH
CP=/var/www/ebaytool/struts2/target/classes/

for jar in `ls /usr/local/apache-tomcat-7.0.2/webapps/ROOT/WEB-INF/lib/`
do
	CP=$CP:/usr/local/apache-tomcat-7.0.2/webapps/ROOT/WEB-INF/lib/$jar
done

start() {
	java -cp $CP ebaytool.apicall.Daemon start \
		>> /var/www/ebaytool/logs/daemon.log &
}

stop() {
	java -cp $CP ebaytool.apicall.Daemon stop
	ps auxw | grep java | grep Daemon
}

case "$1" in
	start)
		start
		;;
	stop)
		stop
		;;
	restart)
		stop
		start
		;;
	*)
		echo $"Usage: $prog {start|stop|restart}"
esac
